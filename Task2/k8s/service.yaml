apiVersion: v1
kind: Service
metadata:
  name: insuretech-demo-svc
  namespace: insurtech
  labels:
    app: insuretech-demo-app
spec:
  selector:
    app: insuretech-demo-app
  ports:
    - name: http
      port: 8080
      targetPort: 9898
      protocol: TCP
  type: NodePort
